# 노트 안정성·장문 AI 생성 품질 점검

검증일: 2026-09-09

## 해결한 원인

| 문제 | 개선 |
| --- | --- |
| 자동 저장이 실패한 뒤에만 초안 생성 | 입력 즉시 세션별 초안 보관, 빈 문자열 수정도 복구 |
| 재연결 자동 재시도 안내와 실제 동작 불일치 | 재연결 저장 및 일시 오류 지수 백오프, 최대 30초 간격 |
| 다른 탭·기기의 이전 버전이 최신 노트를 덮어씀 | 서버의 원자적 revision 비교, 409 충돌 처리와 양쪽 내용 확인 |
| 저장 중 추가 입력, 회의 이동 시 요청·상태 혼선 | 세션 분리, 같은 회의 요청 순서 보장, 최신 입력까지 저장 |
| 브라우저 저장 용량 부족 시 초안 유실 | 오류 표시·Markdown 다운로드, SPA 이동 중 메모리 복구와 닫기 보호 |
| 노트 저장 실패를 무시한 회의 종료 | 저장 성공 후 종료·AI 재생성 진행 |
| 모바일/데스크톱 화면에서 편집기 중복 생성 | 워크스페이스 단일 마운트, 화면 크기·패널 전환 중 상태 유지 |
| 결과를 주제 8개, 복습 항목 5개 등으로 잘라냄 | 의미 있는 추출 항목의 고정 개수 제한 제거 |
| 긴 전사의 중간 부분을 입력에서 제거 | 전체 노트·전사를 빠짐없이 분할하고 추출 결과를 결합 |
| 구간 경계를 회의 종료로 오인 | 명시적 주제 전환을 우선한 분할과 경계 해석 규칙 추가 |
| 첫 구간만 반영한 제목·전체 개요 | 모든 구간의 개요·주제를 바탕으로 전체 메타데이터 재작성 |

## 공개 모범 사례와 적용

1. [OpenAI Cookbook — Summarizing Long Documents](https://cookbook.openai.com/examples/summarizing_long_documents)
   - 긴 문서를 한 번에 요약해도 결과가 입력 길이에 비례해 상세해지지는 않는다고 설명한다.
   - 구간별 요약 후 결합하여 상세도를 조절하는 방법을 제시한다.
   - 적용: 입력 중간 생략 제거, 구간별 독립 출력 예산, 상세 사실은 재요약 없이 결합.
2. [Granola — Customize notes with templates](https://docs.granola.ai/help-center/taking-notes/customise-notes-with-templates)
   - 목적·맥락, 원하는 길이·스타일, 섹션 구조를 명시하고 수치·데이터·인용 등 필요한 상세도를 구체화하도록 안내한다.
   - 적용: 짧은 개요와 상세 본문을 구분하고 배경·근거·대안·수치·사례·질의응답을 보존하는 지시 추가.
3. [Fireflies — AI Meeting Summaries](https://guide.fireflies.ai/articles/9547055509-Fireflies-AI-Meeting-Summaries%3A-View%2C-Customise%2C-Expand%2C-Regenerate)
   - 구조화된 노트, 개요, 액션아이템, 전사에 연결된 타임스탬프 구간을 제공한다.
   - 적용: 주제별 상세 기록과 서버가 계산한 분석 구간 표시. 분석 범위는 개별 문장의 정확한 인용 시점과 구별한다.
   - 타임스탬프 구간 제공 주장은 렌더링 페이지에서 확인했다: `evidence-supported`, confidence `high`.

## 장문 처리 방식

- 기본 구간 크기: `min(12,000자, maxTokens × 2)`, 최소 2,000자. 모델별 토큰 계산기가 아닌 보수적인 문자 기반 분할 기준이다.
- 명시적인 한국어 주제 전환을 우선하고, 없으면 발화 줄 경계에서 나눈다. 아주 긴 단일 발화도 버리지 않는다.
- UTF-16 서로게이트 쌍을 분리하지 않는다. 노트와 전사는 실제 추출 데이터로 각각 한 번씩 포함된다.
- 앞 구간의 마지막 1,000자는 문맥 전용으로 제공하고 반복 추출하지 않도록 지시한다.
- 구간 추출은 최대 2개를 동시에 처리하며 결과 순서는 원문 순서를 유지한다.
- `max_tokens`에 걸린 구간이나 지나치게 빈약한 결과는 입력 구간을 더 작게 나누어 다시 추출한다. 추가 분할은 최대 3단계다.
- 본문은 개수 제한 없이 합친다. 전체 개요·제목만 별도 생성하며, 그 생성에 실패해도 상세 추출 결과는 보존한다.
- 긴 강의의 복습 항목도 모두 렌더링한다. AI 생성이 완전히 실패하면 처음 몇 줄만 보여주는 대신 전사 원문을 보존한다.
- 같은 주제가 여러 구간에 걸쳐 이어질 때는 분석 범위가 다른 항목으로 남을 수 있다. 문장별 근거 검증이나 의미적 중복 제거를 완료했다고 간주하지 않는다.

## 재현 방법

```bash
# backend 디렉터리
pnpm test --runInBand
pnpm test:e2e --runInBand
pnpm exec eslint "{src,test}/**/*.ts"
pnpm exec tsc --noEmit
pnpm build

# frontend 디렉터리
pnpm test
pnpm test:e2e
pnpm lint
pnpm architecture:check
pnpm build
```

브라우저 테스트는 로컬 Next.js 개발 서버와 모의 API를 사용한다. 실제 Toast UI 편집기로
두 탭의 충돌 해결, 새로고침 직전 초안 복구, Markdown 다운로드, 모바일 전환 및 단일 마운트를 검증한다.

### 실제 Bedrock 평가

```bash
# backend 디렉터리, Bedrock 호출 가능한 AWS 자격증명 필요
AWS_BEDROCK_MODEL_ID=global.anthropic.claude-sonnet-5 \
AWS_BEDROCK_MAX_TOKENS=16384 \
pnpm exec ts-node test/evaluate-long-notes.ts
```

- 입력: 108분 타임스탬프를 갖는 합성 한국어 전사 25,560자, 18개 안건, 각각의 수치·담당자·기한, 거절된 아이디어 1개.
- 실제 사용자 노트나 DB를 읽지 않는다. 평가 범위는 전사 이후의 추출·후처리다.
- `EVALUATION_VERBOSE=1`이면 안건별 결과를 출력해 수동 검토할 수 있다.
- 배포 컨테이너에서 `EVALUATE_DEPLOYED_BASELINE=1`을 함께 지정하면 기존 `dist` 구현과 비교한다.
- `coveredTopics`는 식별자의 결과 포함 여부, `completeActions`는 같은 안건 안에서 담당자·기한 일치 여부다. 이 지표만으로 문서 전체의 의미적 정확성을 판단하지 않는다.

### 실측 결과

동일 입력·모델·16,384 토큰 설정으로 배포 중이던 구현과 최종 개선 구현을 호출했다.
기존 구현은 2회 평가 모두 안건 본문 8개와 작업 8개만 남겼다.

| 지표 | 기존 구현 | 최종 개선 구현 |
| --- | ---: | ---: |
| 작업 식별자 포함 | 9 / 18 | 18 / 18 |
| 담당자·기한이 일치하는 작업 | 8 / 18 | 18 / 18 |
| 누락된 기준 수치 | 9 | 0 |
| 추출된 작업 | 8 | 18 |
| 거절된 ZZ-99의 작업 오등록 | 0 | 0 |
| 정규화 후 구조화 JSON 길이 | 4,233자 | 16,496자 |
| 해당 실행 소요 시간 | 82초 | 86초 |

최종 결과의 주제 항목은 19개로, 18개 작업 안건과 별도의 **미채택 아이디어** 항목이다.
전체 제목과 개요는 18개 안건을 반영했고, 중간 구간 NN-09와 경계 구간 NN-17의
담당자·기한도 보존되었다. 초기 분할 구현에서 확인했던 경계 작업 중복은 최종 평가에서 발생하지 않았다.

장문은 구간 수만큼 추가 호출하므로 처리 시간과 비용이 입력량에 따라 증가한다.
위 수치는 동일 합성 사례의 관측값이며 일반적인 응답 시간이나 실제 회의의 정확도 보장값은 아니다.
수치 원본은 [평가 보고서 JSON](./backend/test/fixtures/long-note-evaluation.report.json)에 기록했다.

### 자동 검증 결과

- Backend: 42개 테스트 모음, 325개 테스트 통과.
- Frontend: 41개 테스트 모음, 246개 테스트 통과.
- API 통합 테스트: 11개 통과. 실제 암호화 저장, 경쟁 저장 및 검색 반영 포함.
- Chromium 브라우저 시나리오: 1개 통과. 모의 API와 실제 편집기 사용.
- 양쪽 lint·TypeScript·빌드 통과.
- 프런트엔드 아키텍처 검사: 오류 0개, 파일 크기 경고 8개.

## 배포 연결 사항

PostgreSQL에는 `20260909100000-add-note-revision` 마이그레이션을 적용해야 한다.
노트 GET은 `revision`, PUT은 필수 `expectedRevision`을 사용한다. 기존 클라이언트와의
계약이 달라지므로 프런트엔드·백엔드를 함께 배포한다. 기존 결과는 재생성 시 새 장문 처리 방식을 적용받는다.
